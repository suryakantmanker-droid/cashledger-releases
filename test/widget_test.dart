import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Basic smoke test — real app requires Firebase init, skipped here.
    expect(true, isTrue);
  });
}
